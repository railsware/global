language: ruby

cache:
  bundler: true

rvm:
  - 2.4
  - 2.5
  - 2.6
  - 2.7
  - ruby-head
  - jruby-head

notifications:
  email: false

sudo: false

branches:
  only:
    - master
    - development

matrix:
  allow_failures:
    - rvm: ruby-head
    - rvm: jruby-head
